Short-term:

- fix problem of none environment in the middle of the simu
- add environment dependent weights to favor some likelihood (ex: buildup favor transition with itself)
- adapt time step calculation to match input marker ages
- add unit tests for simulators
- create simulated well from output xarray
- create output plots (acco/accumulation/topo... vs time) (from well in time or from xarray?)
- Wells: allowing to convert Depth to Age from DepthAgeModel composed of markers
  linking depth with age. In between markers, assume a function (default is linear)
- create missing unit tests for save methods
- create export methods to save data objects (simulation, realization, scenario, etc.) where needed (+tests)
- add logger

Long-term:
- create Accumulation models based on Carbonate factories
- define cost function to evaluate misfit between real and simulated well
- implement multi-real framework from parameter ranges
- implement parameter optimisation
- add (de-)compaction law to restore wells at deposition thicknesses
- create dedicated python package for accommodation calculation